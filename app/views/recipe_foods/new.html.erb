<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Ingredient to Recipe</title>
  <%= stylesheet_link_tag 'recipe_food_new', media: 'all', 'data-turbolinks-track': 'reload' %>
</head>
<body>
  <div class="container">
    <%= form_with model: @recipe_food, url: user_recipe_recipe_foods_path(user_id: @recipe.user_id, recipe_id: @recipe.id) do |form| %>
      <h2>Add Ingredient to Recipe</h2>

      <%= form.hidden_field :recipe_id, value: @recipe.id %>

      <div class="field">
        <%= form.label :food_id %>
        <%= form.select :food_id, options_for_select(@foods), {}, { placeholder: "Select a Food" } %>
      </div>

      <div class="field">
        <%= form.label :quantity %>
        <%= form.text_field :quantity %>
      </div>

      <%= form.submit 'Add Ingredient', class: "btn" %>
    <% end %>
  </div>
</body>
</html>

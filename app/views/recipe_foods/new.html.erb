<%= form_with model: @recipe_food, url: user_recipe_foods_path do |form| %>

  <%= form.hidden_field :recipe_id, value: "10" %>

  <div class="field">
    <%= form.label :food_id %>
    <%= form.select :food_id, options_for_select(@foods), {}, { placeholder: "Select a Food" } %>
  </div>

  <div>
    <%= form.label :quantity %>
    <%= form.text_field :quantity %>
  </div>

  <%= form.submit 'Add ingredient' %>
<% end %>

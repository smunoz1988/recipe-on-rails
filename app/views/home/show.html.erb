<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Shopping List</title>
  <%= stylesheet_link_tag 'shopping_list', media: 'all', 'data-turbolinks-track': 'reload' %>
</head>
<body>
  <div class="container">
    <h2>Shopping List</h2>
    <% if @shopping_list[:shopping_list].any? %>
      <ul>
        <% @shopping_list[:shopping_list].each do |food, details| %>
          <li>
            <%= "#{food.name}: #{details[:quantity]} (Total Price: $#{details[:total_price]})" %>
          </li>
        <% end %>
      </ul>
      <p>Total Items to Buy: <%= @shopping_list[:total_items] %></p>
      <p>Total Value Needed: $<%= @shopping_list[:total_value] %></p>
    <% else %>
      <p>No ingredients to buy. Your pantry is fully stocked!</p>
    <% end %>
  </div>
</body>
</html>
